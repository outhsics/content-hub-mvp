# ContentHub MVP - AI 驱动的内容生产系统

一个基于 AI 的自动化内容生产系统，每天自动生成高质量文章，用于多平台发布赚取收益。

## ✨ 核心功能

- 📡 **自动监控**: 13个高质量 RSS 信源，每小时自动抓取最新内容
- 🤖 **AI 评分**: 使用 OpenAI API 自动评分，筛选高质量文章
- 🎨 **智能改写**: 4 种风格改写（头条、知乎、小红书、百家号）
- 📅 **每日生成**: 每天自动生成 30 篇文章（10篇 × 3种风格）
- 📊 **Dashboard**: 简洁的管理界面，一键复制发布

## 🏗️ 技术栈

**后端:**
- Node.js + TypeScript
- Fastify (Web 框架)
- PostgreSQL (Supabase)
- OpenAI API (gpt-4o, gpt-4o-mini)
- node-cron (定时任务)
- rss-parser (RSS 解析)

**前端:**
- Next.js 14 (App Router)
- Tailwind CSS
- Supabase JS Client
- Lucide Icons

## 🚀 快速开始

### 1. 克隆项目

```bash
cd ~/content-hub-mvp
```

### 2. 设置数据库

参考 [backend/db/README.md](./backend/db/README.md) 创建 Supabase 项目并导入 schema。

### 3. 配置环境变量

#### Backend:
```bash
cd backend
cp .env.example .env
# 编辑 .env 填入你的凭证
```

#### Frontend:
```bash
cd frontend
cp .env.local.example .env.local
# 编辑 .env.local 填入你的 Supabase 凭证（与 backend 相同）
```

### 4. 安装依赖

```bash
# Backend
cd backend
npm install

# Frontend
cd ../frontend
npm install
```

### 5. 测试数据库连接

```bash
cd backend
npm test
```

### 6. 生成第一天的内容

```bash
# 手动触发每日生成（测试）
npm run test:daily
```

这将：
1. 从 RSS 信源抓取新文章
2. 使用 AI 评分文章
3. 选择最优质的 5 篇文章
4. 改写成 3 种风格（共 15 篇）
5. 保存到数据库

### 7. 启动服务

**启动后端（后台运行）:**
```bash
cd backend
npm run dev
```

这会启动：
- RSS 抓取器：每小时运行
- 每日生成器：每天 8:00 AM 运行

**启动前端:**
```bash
cd frontend
npm run dev
```

访问 http://localhost:3000 查看 Dashboard。

## 📁 项目结构

```
content-hub-mvp/
├── backend/                 # 后端服务
│   ├── src/
│   │   ├── services/
│   │   │   ├── scraper/    # RSS 抓取服务
│   │   │   ├── ai/         # AI 服务（评分、改写）
│   │   │   └── daily-generator.ts  # 每日生成
│   │   ├── db/             # 数据库连接和 schema
│   │   └── index.ts        # 入口文件
│   ├── db/
│   │   ├── schema.sql      # 数据库结构
│   │   └── seed.sql        # 初始数据
│   └── package.json
├── frontend/               # 前端 Dashboard
│   ├── app/
│   │   └── page.tsx        # 主页面
│   ├── lib/
│   │   └── supabase.ts     # Supabase 客户端
│   └── package.json
└── docs/                   # 架构文档
```

## 🧪 测试命令

```bash
# Backend
cd backend

# 测试数据库连接
npm test

# 测试 RSS 抓取
npm run test:scraper

# 测试 AI 评分
npm run test:ai:score

# 测试标题优化
npm run test:ai:title

# 测试内容改写
npm run test:ai:rewrite

# 测试批量处理
npm run test:ai:batch

# 测试每日生成
npm run test:daily
```

## 📊 使用流程

### 自动化流程（每天）

1. **00:00** - RSS 抓取器开始运行
2. **每小时** - 抓取新文章并保存到数据库
3. **8:00 AM** - 每日生成器启动
   - 评分所有待处理文章
   - 选择最高分的 10 篇
   - 改写成 3 种风格
   - 生成 30 篇文章
4. **9:00 AM** - 你登录 Dashboard
5. **复制发布** - 一键复制文章到各平台

### 手动操作

```bash
# 1. 启动后端
cd backend && npm run dev

# 2. 在另一个终端启动前端
cd frontend && npm run dev

# 3. 触发手动生成
cd backend
npm run test:daily

# 4. 访问 http://localhost:3000 查看结果
```

## 💰 变现策略

### Week 1: 测试阶段
- 每天 10 篇 × 3 个平台 = 30 篇
- 测试不同平台表现
- 优化标题和内容

### Week 2-4: 优化阶段
- 根据数据调整信源优先级
- 保留高收益模板
- 目标：每天 50-100 元收益

### Month 2: 扩展阶段
- 添加更多平台
- 增加信源数量（20+）
- 优化 AI Prompt

## 🎯 下一步功能（Phase 2+）

- [ ] BullMQ + Redis 任务队列
- [ ] 今日头条自动发布 API
- [ ] Puppeteer 集成（支持更多平台）
- [ ] 收益追踪 Dashboard
- [ ] A/B 测试标题
- [ ] 多账号管理

## ⚠️ 注意事项

1. **OpenAI API 成本**
   - 评分：~$0.001/篇
   - 改写：~$0.01-0.03/篇
   - 每天 30 篇 ≈ $0.30-0.90/天
   - 建议：设置月度预算上限

2. **平台规则**
   - 避免频繁发布
   - 确保内容原创性
   - 遵守各平台社区规范

3. **内容质量**
   - 定期检查生成质量
   - 必要时手动编辑
   - 删除低质量文章

## 📝 License

MIT

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

---

**祝你早日实现被动收入！** 🚀💰
